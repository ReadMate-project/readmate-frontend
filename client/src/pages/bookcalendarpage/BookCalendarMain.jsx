import React from 'react';
import { Outlet } from 'react-router';

const BookCalendarMain = () => {
    return (
        <div>
            <Outlet/>
        </div>
    );
};

export default BookCalendarMain;